package org.marssquad.vmsys.ui.nav

/*
@Composable
fun AppNav() {
    val navigator = rememberNavigator()
    val modifier: Modifier = Modifier

    NavHost(
        navigator = navigator,
        initialRoute = AppScreenRoutes.Dashboard.route,
        modifier = modifier.fillMaxSize()
    ) {
        // Dashboard Screen
        scene(route = AppScreenRoutes.Dashboard.route){
            DashboardScreen()
        }

        // Work Orders Screen
        scene(route = AppScreenRoutes.Dashboard.route){
            WorkOrdersScreen()
        }

        // Vendors List Screen
        scene(route = AppScreenRoutes.Dashboard.route){
            VendorsListScreen()
        }

        // Financials Screen
        scene(route = AppScreenRoutes.Dashboard.route){
            FinancialsScreen()
        }

        // Settings Screen
        scene(route = AppScreenRoutes.Dashboard.route){
            SettingsScreen()
        }
    }
}*/

